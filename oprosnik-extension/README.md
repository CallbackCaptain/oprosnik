# Oprosnik Helper v5.0

Chrome-расширение для автоматизации заполнения опросников CTP с данными из Cisco Finesse.

## Что нового в v5.0

### Оптимизация кода
- **Сокращение файлов**: 4 content-скрипта → 2 (`survey-page.js`, `finesse-page.js`)
- **Удалён React**: popup теперь на чистом HTML/JS (~15KB вместо ~200KB)
- **Модульная архитектура**: код разбит на логические модули
- **MutationObserver**: вместо `setInterval` для отслеживания DOM-изменений

### Улучшения
- Чистый, читаемый код с единым стилем
- Улучшенная обработка ошибок
- Более надёжный поиск элементов
- Современный градиентный UI

### Упрощённый Popup
- Кнопка "Открыть опросник"
- Кнопка "Скрыть/Показать сайдбар"  
- Индикаторы статуса (мониторинг, Finesse)
- Информация о звонках

## Структура файлов

```
oprosnik-extension/
├── manifest.json          # Конфигурация расширения
├── background.js          # Service Worker (мониторинг Finesse)
├── popup.html            # UI popup'а
├── popup.js              # Логика popup'а
├── scripts/
│   ├── survey-page.js    # Скрипт для страницы опросника (CTP)
│   └── finesse-page.js   # Скрипт для страницы Finesse
├── css/
│   └── styles.css        # Все стили
└── icons/
    ├── icon16.png
    ├── icon48.png
    └── icon128.png
```

## Установка

1. Открой `chrome://extensions/`
2. Включи "Режим разработчика"
3. Нажми "Загрузить распакованное расширение"
4. Выбери папку `oprosnik-extension`

## Использование

1. Открой Cisco Finesse — расширение начнёт мониторинг автоматически
2. После завершения звонка данные сохранятся
3. На странице опросника (ctp.rt.ru) нажми "📞 Вставить данные"
4. Выбери нужный звонок из истории

## Debug

В консоли браузера:
- `monitorStatus()` — статус мониторинга (на странице Finesse)
- `forceCheck()` — принудительная проверка статуса
- `debugOprosnik()` — диагностика (на странице опросника)

## Сравнение версий

| Метрика | v4.x | v5.0 |
|---------|------|------|
| Файлов JS | 6 | 4 |
| Строк кода | ~1500 | ~500 |
| Зависимости | React, etc. | Нет |
| Размер popup | ~200KB | ~15KB |
